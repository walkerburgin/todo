# todo-components

This package contains a todo list item component.

NOTE: This is pretty contrived -- in a real project you probably wouldn't put a component like this in
its own package. But for the purposes of the presentation it helps to illustrate a small frontend monorepo.
